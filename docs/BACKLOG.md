# 藏知当前任务清单

更新时间：2026-09-15

本文件只维护尚未完成或需要持续验证的工作。已交付能力统一记录在
[路线图的已交付基线](ROADMAP.md#1-已交付基线)，不再把历史开发日志混入当前任务。

优先级含义：

- `P0`：个人 Beta 的可靠性或质量阻塞项
- `P1`：个人 Beta 的重要体验
- `P2`：后续增强

## P0：处理状态与可观察性

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-Q03 | 任务统计 | 可查看各阶段耗时、失败类型、重试次数和积压数量 |
| CZ-Q04 | 大文档回归集 | PDF、Word、Markdown、代码、表格和异常编码均有可重复测试样本 |
| CZ-Q05 | Worker 超时任务恢复 | 任务按执行容量领取，具备心跳与租约；进程中断后可恢复，并验证重复执行不会破坏派生产物 |

CZ-Q05 分阶段公平调度与单任务领取已合并发布，并通过正式环境推进验收。
剩余验收仍包括心跳、租约、崩溃恢复及同版本安全并发；不能因此将整个任务标记完成。

CZ-Q06 已交付：DOCX 正文表格原序与真实标题级别栈；完整 API/Worker 680 项通过。
复杂单元格与文档地图后续由 CZ-N01 承接，不自动重建历史资料。

CZ-Q07 收件箱首批性能修复已完成：服务端分页筛选、精简状态投影和串行刷新。
更大规模时需继续评估持久化状态汇总；目前每次请求仍按空间扫描轻量状态，
不是数据库内完整聚合，成本仍随文档与切片数量增长。

CZ-Q01、CZ-Q02 已交付：知识库与处理中心共用统一阶段口径；向量覆盖按当前
profile、切片内容哈希和数据集抽样策略计算，并可定向批量补建。

## P0：检索质量

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-R01 | 黄金问题集 | 至少 30 个真实问题，标注正确文档、chunk 和来源位置 |
| CZ-R02 | 分路评测 | 输出全文、向量、混合检索的 Top 5 命中率 |
| CZ-R03 | 切片诊断 | 能识别标题孤立、表格破坏、语义截断和重复 overlap |
| CZ-R04 | 引用质量 | 回答引用覆盖率与定位成功率可自动检查 |
| CZ-R05 | Reranker 决策 | 仅在黄金集证明收益且降级安全时启用 |

## P0：外部来源稳定性

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-S01 | WebDAV 定时扫描 | 可配置低频计划，记录每次成功、失败和结果摘要 |
| CZ-S02 | 增量导入控制 | 大目录可断点续扫、限速，单个失败不阻塞其他文件 |
| CZ-S03 | 来源诊断 | 页面展示待同步、待确认缺失、已回收、保留快照和失败文件 |
| CZ-S04 | 网页抓取恢复 | 区分拦截、超时、正文为空、类型不支持并给出正确重试建议 |

## 文档理解演进（v1.0 之后）

执行顺序与边界见 [文档理解计划](DOCUMENT_UNDERSTANDING_PLAN.md)。本节列完整目标，
部分子项已交付，仍不能作为当前公共 API 能力说明；首批结构修复沿用 CZ-Q06。
CZ-N01/N03 的地图、窗口持久化、预算/继续/取消和管理 UI 已有代码与隔离测试；
CZ-N05 已接入增强产物的 REST/MCP/CLI/Skill 分窗只读探索；CZ-N03 新增可选的
预算内连续窗口分层概览与原文下钻。语义章节汇总、跨窗口实体消歧、真实质量评测
及语义章节/单元格级完整探索不能标记完成。原文标题/块分页读取已接入统一接口，
不依赖增强，具有版本指纹和读取上限；仍不是数据库块级缓存。见
[增强使用说明](KNOWLEDGE_ENHANCEMENT.md)。

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-N01 | 文档地图与表格对象 | 版本内结构身份、原序、章节与单元格结构可定位；未识别结构明确标记 |
| CZ-N02 | 派生产物版本与评测 | 候选构建/原子启用/旧引用保留，通过至少 30 个跨类型黄金问题对照 |
| CZ-N03 | 分层 AI 理解 | 文档/章节/局部覆盖、可追溯关系、缓存、成本上限、失败降级均可验收 |
| CZ-N04 | AI 辅助切片 | 块 ID 与边界提议通过覆盖和完整性校验；不改写原文，评测有收益才启用 |
| CZ-N05 | 统一探索工具 | 先 ADR 后实现目录/章节/上下文/表格读取；所有入口共享预算与权限交集 |
| CZ-N06 | 人工纠正与迁移 | 候选差异、纠正优先级、回滚和覆盖可见；历史重建须明确发起 |

## P1：交互与管理

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-U01 | 问答记录 | 已支持服务端保存问题、回答、知识范围和实际引用；默认不作为模型记忆 |
| CZ-U02 | 记录管理 | 已支持回看和删除；待补记录重命名、归档、搜索和按原范围重跑 |
| CZ-U03 | 批量知识操作 | 分类、标签、删除、恢复和重处理使用一致的选择与确认逻辑 |
| CZ-U04 | AI 整理建议 | AI 可生成待确认的标签合并、分类调整和重复资料建议 |
| CZ-U05 | 知识详情收敛 | 高频内容和操作优先，处理元数据按需展开 |

个人多工作空间的首阶段已交付。后续若进入多用户，必须单独设计成员关系、角色、
空间级授权、PAT 空间授权和审计，不能把当前管理员账号简单复制成多账号。

## P1：数据与运维

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-O01 | 自动备份计划 | 定时生成、校验、轮换备份并显示最近成功时间 |
| CZ-O02 | 恢复向导 | 在不误覆盖生产数据的前提下指导校验和恢复 |
| CZ-O03 | 模型成本统计 | 按任务查看 token、费用估算、耗时和失败率 |
| CZ-O04 | 安全审计 | PAT 调用记录身份、路由、状态和耗时，不记录正文与令牌 |

## P2：新增入口与领域能力

| ID | 任务 | 完成定义 |
|---|---|---|
| CZ-I01 | 邮件入库 | 邮件及附件具备稳定身份、幂等和明确删除语义 |
| CZ-I02 | 浏览器扩展 | 一键收藏当前页面并查看后台处理状态 |
| CZ-D01 | OCR 第一阶段（基线 H） | 已交付扫描 PDF 文字兜底；后续图片语义理解、图表与公式识别仍待跟进 |
| CZ-D01b | OCR 第二阶段（基线 H） | 已交付可插拔的外部视觉 OCR；本地 tesseract 仍是必选，外部仅在本地失败/低置信度时触发；超限自动降级本地 |
| CZ-D02 | 法律/合同 Profile | 保留条款结构并提取领域字段，通用流程仍可降级 |
| CZ-D03 | 论文/会议 Profile | 提取问题、方法、结论、决策和待办等结构 |
| CZ-D04 | 图片语义理解 | 在 OCR 第二阶段基础上识别图片内容、版面与图表/公式 |
| CZ-G01 | 关系发现 | 在基础检索评测稳定后提供相似、冲突和专题关系 |

CZ-I03 已交付：REST 文件上传使用独立 `documents:write` 权限，以 `external_id`
实现可重复提交和版本更新，并返回可由上传令牌查询的处理状态地址；文档 Scope Key
支持单篇与批量整体同步。该能力不包含删除、任意 SQL 或 MCP 写操作。

## 完成规则

- 代码、迁移、测试、用户可见状态和文档必须一起交付。
- 任何新来源都必须验证新增、更新、删除、恢复和永久排除。
- 任何检索优化都必须经过黄金集比较，不能只凭单个示例判断。
- 任何不可逆操作必须经过影响预览和明确确认。
